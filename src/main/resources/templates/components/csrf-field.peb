{# @pebvariable name="_csrf" type="org.springframework.security.web.csrf.CsrfToken" #}

<label>
    <input hidden class="is-hidden" name="{{ _csrf.parameterName }}" value="{{ _csrf.token }}">
</label>
